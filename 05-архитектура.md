# 05. Архитектура

## Общий подход

Система строится как специализированный пользовательский канал поверх цифровой платформы умного вокзала. Главная логика сосредоточена не в хранении карты и не в оркестрации всего пассажирского пути, а в диалоге, уточнении контекста, адаптации платформенных подсказок и сервисной эскалации.

## Контейнеры

```mermaid
C4Container
    title Контейнерная архитектура MVP
    Person(passenger, "Пассажир", "Использует экран робота или веб-интерфейс")
    Person(staff, "Сотрудник вокзала", "Просматривает обращения")
    Person(admin, "Администратор", "Настраивает тестовые данные")

    System_Boundary(system, "Система робота-стюарда") {
        Container(web, "Robot Interaction UI", "Streamlit/React", "Диалог, кнопки, экран робота")
        Container(admin_ui, "Административный UI", "Streamlit/React", "Журнал обращений, справочники, аналитика")
        Container(api, "Steward Backend API", "FastAPI", "Единая точка входа для UI робота")
        Container(dialogue, "Dialogue and Scenario Service", "Python", "Состояние диалога и формирование ответа")
        Container(platform_adapter, "Smart Station Platform Adapter", "Python", "Контракт с платформой или mock-данными")
        Container(help, "Assistance Service", "Python", "Обращения к персоналу")
        Container(analytics, "Interaction Analytics Service", "Python", "События диалога, отчеты, частые запросы")
        ContainerDb(db, "Robot DB", "PostgreSQL/SQLite", "Диалоги, обращения, события, mock-ответы")
    }

    System_Ext(platform, "Цифровая платформа умного вокзала", "Карта, рейсы, маршруты, подсказки")

    Rel(passenger, web, "Работает с интерфейсом")
    Rel(staff, admin_ui, "Просматривает обращения")
    Rel(admin, admin_ui, "Настраивает данные")
    Rel(web, api, "HTTP/JSON")
    Rel(admin_ui, api, "HTTP/JSON")
    Rel(api, dialogue, "Запрос сопровождения")
    Rel(dialogue, platform_adapter, "Контекст рейса, маршрут, подсказки")
    Rel(platform_adapter, platform, "API платформы или mock")
    Rel(api, help, "Создание обращения")
    Rel(api, analytics, "Запись событий")
    Rel(dialogue, db, "Хранит состояние диалога")
    Rel(platform_adapter, db, "Читает mock-ответы в автономном MVP")
    Rel(help, db, "Пишет обращения")
    Rel(analytics, db, "Пишет события")
```

## Компоненты Dialogue and Scenario Service

```mermaid
C4Component
    title Компоненты сценарного сервиса робота
    Container_Boundary(dialogue, "Dialogue and Scenario Service") {
        Component(request, "Passenger Request Parser", "Python", "Определяет намерение пассажира")
        Component(context, "Passenger Context Builder", "Python", "Собирает поезд, цель, ограничения и язык")
        Component(platform, "Platform Query Client", "Python", "Запрашивает маршрут и подсказки")
        Component(policy, "Steward Response Policy", "Python", "Выбирает форму ответа и fallback")
        Component(explain, "Route and Hint Explainer", "Python", "Формирует понятные шаги и сообщения")
        Component(escalation, "Escalation Decider", "Python", "Решает, когда предложить помощь персонала")
    }

    Rel(request, context, "Собирает контекст")
    Rel(context, platform, "Запрашивает данные платформы")
    Rel(platform, policy, "Передает маршрут, подсказки и ограничения")
    Rel(policy, explain, "Выбирает ответ")
    Rel(policy, escalation, "Проверяет необходимость помощи")
```

## Основные политики

| Политика | Описание |
| --- | --- |
| Политика маршрутизации | Робот запрашивает у платформы маршрут с нужным режимом и объясняет результат |
| Политика достоверности | При отсутствии данных поездки система показывает предупреждение и не выдает выдуманную информацию |
| Политика доступности | Робот не показывает маршрут как пригодный, если платформа вернула ограничение доступности |
| Политика интеграций | Цифровая платформа и система заявок подключаются через адаптеры |
| Политика аналитики | События собираются без персональных данных |

## Ключевые интерфейсы

- `POST /steward/dialogue` - обработать реплику или действие пассажира.
- `POST /steward/route-explanation` - получить человекочитаемое объяснение маршрута.
- `GET /steward/trip/{train_number}` - получить адаптированную для пассажира информацию по поездке.
- `POST /assistance/request` - создать обращение.
- `GET /analytics/summary` - получить агрегированную статистику.
- `GET /platform-adapter/health` - проверить доступность платформы или mock-адаптера.

## Технологический стек MVP

- Python - доменная логика.
- FastAPI - backend API.
- Локальные JSON/SQLite mock-данные - автономная имитация платформы для демонстрации.
- PostgreSQL - целевое хранилище диалогов, обращений и событий; SQLite допустим для локальной демонстрации.
- Streamlit или простой web UI - быстрый демонстрационный интерфейс.
